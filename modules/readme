1.Copy and paste template.py and change the class name.

2. make use of the @makeCommand(name="Name", description="")
    instead of overriding msg for ease of use.
    a command will work like so
    .hello hello world
    a Command object is then recieved.


* You may delete any of the websocket events you are not
using to keep your code short and clean.
